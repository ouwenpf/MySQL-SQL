# 基础查询

## 排序查询
```
select 
		查询列表
from
		表名
where	
		筛选条件
		order by 排序列表 asc|desc
特点：
		1. asc表示是升序，desc表示是降序；如果都不写表示是升序
		2. order by子句中可以支持单个字段，多个字段，表达式，函数，别名
		3. order by子句一般是放在查询语句最后面，limit字句除外 


			 
```